machine:
  environment:
    _JAVA_OPTIONS: "-Xms512m -Xmx1024m"
  java:
    version: oraclejdk7

dependencies:
  override: 
    - java -XX:+PrintFlagsFinal -version
    - mvn --version
    - mvn dependency:resolve-plugins dependency:resolve -e

test:
  override: 
    - mvn clean verify -e
